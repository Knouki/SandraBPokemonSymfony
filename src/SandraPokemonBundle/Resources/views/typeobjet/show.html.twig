{% extends 'base.html.twig' %}

{% block body %}
    <h1>TypeObjet</h1>

    <table>
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ typeObjet.nom }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ typeObjet.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typeobjet_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typeobjet_edit', { 'id': typeObjet.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
