{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pokedex list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pokedex in pokedexes %}
            <tr>
                <td><a href="{{ path('pokedex_show', { 'id': pokedex.id }) }}">{{ pokedex.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('pokedex_show', { 'id': pokedex.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('pokedex_edit', { 'id': pokedex.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pokedex_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
